<UserControl x:Class="OLED_Sleeper.Views.MonitorConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:OLED_Sleeper.ViewModels"
             xmlns:converters="clr-namespace:OLED_Sleeper.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:MonitorConfigurationViewModel}"
             FontFamily="Bahnschrift">

    <UserControl.Resources>
        <converters:EqualityToBooleanConverter x:Key="EqualityToBooleanConverter"/>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Box" Width="18" Height="18" BorderThickness="1" CornerRadius="3" 
                                    BorderBrush="#8E8E93" Background="Transparent">
                                <Viewbox Margin="2">
                                    <Path x:Name="CheckMark" Data="M 0,5 L 4,9 L 10,1" StrokeThickness="2" Stroke="#007BFF" Visibility="Collapsed"/>
                                </Viewbox>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Grid Width="18" Height="18">
                                <Ellipse Stroke="#8E8E93" StrokeThickness="1"/>
                                <Ellipse x:Name="CheckMark" Fill="#007BFF" Width="10" Height="10" Visibility="Collapsed"/>
                            </Grid>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter TargetName="CheckMark" Property="Fill" Value="#8E8E93"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border x:Name="Track" Height="4" Background="#5A5A5C" CornerRadius="2"/>
                            <Canvas>
                                <Border x:Name="PART_SelectionRange" Background="#007BFF" Height="4" CornerRadius="2"/>
                            </Canvas>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Width="16" Height="16" Background="#F5F5F5" BorderBrush="#8E8E93" BorderThickness="1">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Thumb" Property="BorderBrush" Value="#007BFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="25,20">
        <StackPanel>
            <TextBlock Text="{Binding MonitorTitle}" Foreground="#F5F5F5" FontSize="18" FontWeight="SemiBold"/>
            <Separator Background="#4A4A4C" Margin="0,12"/>

            <CheckBox Content="Manage this monitor" Style="{StaticResource ModernCheckBox}" IsChecked="{Binding IsManaged}"/>

            <StackPanel Margin="0,20,0,0" IsEnabled="{Binding IsManaged}">
                <TextBlock Text="BEHAVIOR" Foreground="#8E8E93" FontSize="12" FontWeight="Light" Margin="0,0,0,5"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <RadioButton Content="Blackout" GroupName="Behavior" Style="{StaticResource ModernRadioButton}"
                                 IsChecked="{Binding Behavior, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Blackout}"/>
                    <RadioButton Content="Dim" GroupName="Behavior" Style="{StaticResource ModernRadioButton}" Margin="25,0,0,0"
                                 IsChecked="{Binding Behavior, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Dim}"/>
                </StackPanel>

                <StackPanel Margin="0,20,0,0" IsEnabled="{Binding IsDimSliderEnabled}">
                    <TextBlock Text="DIM LEVEL" Foreground="#8E8E93" FontSize="12" FontWeight="Light"/>
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Slider Grid.Column="0" Minimum="0" Maximum="100" Value="{Binding DimLevel}" Style="{StaticResource ModernSlider}" TickFrequency="1"/>

                        <TextBlock Grid.Column="1" Foreground="#F5F5F5" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock.Text>
                                <Binding Path="DimLevel" StringFormat="{}{0:F0}%"/>
                            </TextBlock.Text>
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>